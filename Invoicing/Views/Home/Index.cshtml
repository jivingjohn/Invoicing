@model List<IQueryable<Invoicing.Models.BaseModel>>

@{
    ViewData["Title"] = "Home Page";
}

@foreach (IQueryable<Invoicing.Models.BaseModel> table in Model)
{
    @await Html.PartialAsync("IQueryableTable", table)
}